# SmartVision
SmartVision is a deepfake detection demo app that classifies images as real or AI-generated using a Vision Transformer (ViT). It leverages self-attention to capture global patterns and detect subtle artifacts, showcasing transformer-based models for image authenticity verification.
